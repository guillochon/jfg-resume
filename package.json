{
  "name": "jfg-resume",
  "private": true,
  "version": "1.0.0",
  "description": "James Guillochon resume build environment",
  "scripts": {
    "setup": "playwright install chromium",
    "build": "node scripts/render-resume.mjs",
    "watch": "node scripts/render-resume.mjs --watch",
    "build:pages": "node scripts/render-resume.mjs"
  },
  "devDependencies": {
    "@resumx/resumx": "^0.1.0",
    "playwright": "^1.59.1"
  }
}
